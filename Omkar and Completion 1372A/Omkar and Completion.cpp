#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;

        while(n--)
        {
            cout << "1 ";    /// any number would do (1 <= n <= 1000)
        }

        puts("");
    }

    return 0;
}

/// critical thinking
