#include<iostream>
using namespace std;
int main()
{
    // int n, sum=0;
    // cin >> n;
    // string polyhedrons;

    // for(int i=0 ; i<n ; i++)
    // {
    //     cin >> polyhedrons;

    //     ( polyhedrons[0] == 'T' ? sum+=4 : (polyhedrons[0] == 'C' ? sum+=6 : (polyhedrons[0] == 'O' ? sum+=8 : (polyhedrons[0] == 'D' ? sum+=12 : sum+=20 ) ) ) );
    // }

    cout << "he" << endl;

    return 0;
}
